# Universal Zsh Aliases
# Cross-device development aliases

# Basic commands
alias ll='ls -la'
alias la='ls -A'
alias l='ls -CF'
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias .....='cd ../../../..'

# Git aliases
alias g='git'
alias ga='git add'
alias gc='git commit'
alias gp='git push'
alias gs='git status'
alias gl='git log --oneline'
alias gd='git diff'
alias gb='git branch'
alias gco='git checkout'
alias gst='git stash'
alias gsp='git stash pop'
alias gf='git fetch'
alias gm='git merge'
alias gr='git rebase'
alias gcl='git clone'
alias gpl='git pull'
alias gpu='git push -u origin'
alias gaa='git add .'
alias gcm='git commit -m'
alias gca='git commit --amend'
alias gcan='git commit --amend --no-edit'
alias gcp='git cherry-pick'
alias grh='git reset --hard'
alias grs='git reset --soft'
alias gclean='git clean -fd'
alias gprune='git remote prune origin'

# Development aliases
alias n='nvim'
alias v='nvim'
alias e='nvim'
alias c='code'
alias o='open'
alias t='tmux'
alias lg='lazygit'
alias tt='timetrap'
alias lc='leetcode-cli'
alias tk='task'
alias http='httpie'

# Project navigation
alias dev='cd ~/dev-env'
alias code='cd ~/code'
alias sfcc='cd ~/code/sfcc-project'
alias next='cd ~/code/nextjs-app'
alias leet='cd ~/code/leetcode'
alias docs='cd ~/docs'

# SFCC specific
alias sfcc-clean='npm run clean'
alias sfcc-upload='dwupload'
alias sfcc-start='npm start'
alias sfcc-test='npm test'
alias sfcc-build='npm run build'

# Next.js specific
alias next-dev='npm run dev'
alias next-build='npm run build'
alias next-start='npm start'
alias next-test='npm test'
alias next-lint='npm run lint'

# System aliases
alias c='clear'
alias h='history'
alias j='jobs'
alias f='fg'
alias k='kill'
alias p='ps'
alias u='uptime'
alias w='who'
alias df='df -h'
alias du='du -h'
alias free='free -h'
alias top='htop'
alias tree='tree -C'

# Network aliases
alias ping='ping -c 5'
alias ports='netstat -tuln'
alias myip='curl -s https://ipinfo.io/ip'
alias weather='curl wttr.in'
alias weather2='curl -s wttr.in/2'

# File operations
alias cp='cp -i'
alias mv='mv -i'
alias rm='rm -i'
alias mkdir='mkdir -p'
alias rmdir='rmdir'
alias chmod='chmod -R'
alias chown='chown -R'
alias find='find . -name'
alias grep='grep --color=auto'
alias fgrep='fgrep --color=auto'
alias egrep='egrep --color=auto'

# Archive operations
alias untar='tar -xvf'
alias tar='tar -cvf'
alias zip='zip -r'
alias unzip='unzip -l'
alias 7z='7z a'
alias un7z='7z x'

# Mobile specific aliases (Android/iOS)
if [[ "$OSTYPE" == "android"* ]] || [[ -f /data/data/com.termux/files/usr/bin/termux-info ]]; then
  # Android/Termux
  alias termux='termux-open'
  alias share='termux-share'
  alias clipboard='termux-clipboard-set'
  alias vibrate='termux-vibrate'
  alias torch='termux-torch'
  alias camera='termux-camera-photo'
  alias location='termux-location'
  alias sensor='termux-sensor'
  alias battery='termux-battery-status'
  alias wifi='termux-wifi-scaninfo'
  alias usb='termux-usb'
  alias audio='termux-microphone-record'
  alias notification='termux-notification'
  alias wallpaper='termux-wallpaper'
  alias volume='termux-volume'
  alias brightness='termux-brightness'
  alias keyboard='termux-keyboard'
  alias touch='termux-touch'
  alias gesture='termux-gesture'
fi

# iOS/iSH specific
if [[ -f /etc/alpine-release ]]; then
  # iSH specific aliases
  alias ios='open'
  alias share='open'
  alias clipboard='pbcopy'
  alias vibrate='echo "Vibration not supported in iSH"'
  alias camera='echo "Camera not supported in iSH"'
  alias location='echo "Location not supported in iSH"'
  alias sensor='echo "Sensors not supported in iSH"'
  alias battery='echo "Battery info not available in iSH"'
  alias wifi='echo "WiFi info not available in iSH"'
  alias usb='echo "USB not supported in iSH"'
  alias audio='echo "Audio not supported in iSH"'
  alias notification='echo "Notifications not supported in iSH"'
  alias wallpaper='echo "Wallpaper not supported in iSH"'
  alias volume='echo "Volume control not supported in iSH"'
  alias brightness='echo "Brightness control not supported in iSH"'
  alias keyboard='echo "Keyboard control not supported in iSH"'
  alias touch='echo "Touch not supported in iSH"'
  alias gesture='echo "Gestures not supported in iSH"'
fi

# Development workflow aliases
alias start='~/dev-env/scripts/daily.sh'
alias mobile='~/dev-env/scripts/mobile.sh'
alias sync='~/dev-env/scripts/sync.sh'
alias backup='~/dev-env/scripts/backup.sh'
alias restore='~/dev-env/scripts/restore.sh'

# Quick development commands
alias serve='python3 -m http.server'
alias serve-php='php -S localhost:8000'
alias serve-node='npx serve'
alias serve-ruby='ruby -run -e httpd . -p 8000'

# Database aliases
alias mysql='mysql -u root -p'
alias psql='psql -U postgres'
alias mongo='mongosh'

# Docker aliases
alias d='docker'
alias dc='docker-compose'
alias dps='docker ps'
alias dpa='docker ps -a'
alias di='docker images'
alias dr='docker run'
alias de='docker exec'
alias dk='docker kill'
alias drm='docker rm'
alias drmi='docker rmi'
alias dbu='docker build'
alias dpu='docker push'
alias dpl='docker pull'
alias dlo='docker logs'
alias dsto='docker stop'
alias dst='docker start'
alias drs='docker restart'

# Kubernetes aliases
alias k='kubectl'
alias kg='kubectl get'
alias kd='kubectl describe'
alias kdel='kubectl delete'
alias kex='kubectl exec'
alias klo='kubectl logs'
alias kpf='kubectl port-forward'
alias kctx='kubectl config use-context'
alias kns='kubectl config set-context --current --namespace'

# AWS aliases
alias aws='aws'
alias s3='aws s3'
alias ec2='aws ec2'
alias rds='aws rds'
alias lambda='aws lambda'
alias cloudformation='aws cloudformation'
alias iam='aws iam'
alias sts='aws sts'

# Azure aliases
alias az='az'
alias azure='az'
alias azgroup='az group'
alias azvm='az vm'
alias azstorage='az storage'
alias azwebapp='az webapp'
alias azsql='az sql'
alias azcosmos='az cosmosdb'

# Google Cloud aliases
alias gcloud='gcloud'
alias gs='gsutil'
alias gke='gcloud container'
alias gce='gcloud compute'
alias gsql='gcloud sql'
alias gstorage='gsutil'
alias gfunctions='gcloud functions'

# Monitoring and debugging
alias logs='tail -f'
alias follow='tail -f'
alias watch='watch -n 1'
alias monitor='htop'
alias processes='ps aux'
alias ports='netstat -tuln'
alias connections='netstat -an'
alias routes='route -n'
alias interfaces='ip addr show'
alias memory='free -h'
alias disk='df -h'
alias cpu='top'
alias load='uptime'
alias users='who'
alias sessions='w'
alias last='last -n 10'

# Security aliases
alias ssh='ssh -o StrictHostKeyChecking=no'
alias scp='scp -o StrictHostKeyChecking=no'
alias rsync='rsync -avz'
alias backup='rsync -avz --delete'
alias sync='rsync -avz'
alias encrypt='gpg --symmetric'
alias decrypt='gpg --decrypt'
alias hash='sha256sum'
alias checksum='md5sum'
alias verify='gpg --verify'

# Text processing aliases
alias count='wc -l'
alias lines='wc -l'
alias words='wc -w'
alias chars='wc -c'
alias sort='sort'
alias unique='sort | uniq'
alias reverse='tac'
alias head='head -n 20'
alias tail='tail -n 20'
alias grep='grep --color=auto'
alias search='grep -r'
alias find='find . -name'
alias locate='locate'
alias which='which'
alias where='whereis'
alias type='type'
alias alias='alias'

# Archive and compression aliases
alias tar='tar -cvf'
alias untar='tar -xvf'
alias gzip='gzip'
alias gunzip='gunzip'
alias zip='zip -r'
alias unzip='unzip'
alias 7z='7z a'
alias un7z='7z x'
alias rar='rar a'
alias unrar='rar x'

# Media aliases
alias play='ffplay'
alias convert='ffmpeg'
alias image='feh'
alias video='mpv'
alias audio='mpv'
alias music='mpv'
alias podcast='mpv'
alias stream='ffmpeg -f v4l2 -i /dev/video0 -f alsa -i hw:0 -c:v libx264 -c:a aac -f flv rtmp://live.twitch.tv/live/your_key'

# Network aliases
alias ping='ping -c 5'
alias trace='traceroute'
alias route='ip route'
alias arp='arp -a'
alias dns='nslookup'
alias dig='dig'
alias whois='whois'
alias nmap='nmap'
alias netstat='netstat -tuln'
alias ss='ss -tuln'
alias iptables='iptables -L'
alias firewall='ufw status'
alias ports='netstat -tuln'
alias connections='ss -tuln'
alias bandwidth='iftop'
alias traffic='nethogs'
alias monitor='nload'
alias speed='speedtest-cli'
alias test='ping -c 4 8.8.8.8'

# System information aliases
alias info='uname -a'
alias kernel='uname -r'
alias os='cat /etc/os-release'
alias version='cat /etc/version'
alias uptime='uptime'
alias load='uptime'
alias memory='free -h'
alias disk='df -h'
alias cpu='lscpu'
alias hardware='lshw'
alias pci='lspci'
alias usb='lsusb'
alias modules='lsmod'
alias services='systemctl list-units'
alias processes='ps aux'
alias jobs='jobs'
alias history='history'
alias last='last -n 10'
alias who='who'
alias users='w'
alias sessions='w'
alias login='last -n 10'
alias reboot='sudo reboot'
alias shutdown='sudo shutdown'
alias poweroff='sudo poweroff'
alias halt='sudo halt'
alias suspend='sudo systemctl suspend'
alias hibernate='sudo systemctl hibernate'
alias sleep='sudo systemctl sleep'

